# "docker-machine" options
export DIGITALOCEAN_SIZE=2gb    # 2 GB RAM, 2 vCores, 40 GB SSD, 3 TB transfer

# "cloud" options
export SWAP_SIZE=4G             # make a swap file just in case things get crazy
export RAMDISK_SIZE=512M        # allow the minecraft world to be up to 512 MB
