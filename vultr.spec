# "docker-machine" options
export VULTR_REGION=2           # Chicago
export VULTR_PLAN=94            # 2048 MB RAM,45 GB SSD,3.00 TB BW

# "cloud" options
export SWAP_SIZE=0              # swap is difficult to enable on RancherOS, so don't try
export RAMDISK_SIZE=512M        # allow the minecraft world to be up to 512 MB
